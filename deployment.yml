apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  namespace: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      name: nginx-deployment-pod
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
      #   volumeMounts:
      #     - mountPath: /var/www/html
      #       name: local-pvc
      # volumes:
      # - name: local-pv
      #   persistentVolumeClaim:
      #     claimName: local-pvc
